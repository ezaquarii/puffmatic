export PATH=${PATH}:$(pwd)/bin
export PYTHONPATH=$(pwd)/src/

# This directory should be on a filesystem mounted with noperm flag.
export PUFFMATIC_TEST_WORK_DIR=/release/puffmatic/

if [ -d venv ]; then
    source ./venv/bin/activate
fi

unset PS1

